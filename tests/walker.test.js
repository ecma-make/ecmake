require('chai').should();
const { Walker } = require('..'); // eslint-disable-line no-unused-vars

describe('Walker', () => {
  describe('constructor', () => {
    it.skip('should create a Walker', () => {
    });
  });
});
